sudo useradd <username> # -d
sudo usermod <username> # sudo usermod -aG sudo username
sudo userdel -r <username>
sudo passwd -l <username> # locking -u to unlock
# /etc/passwd /etc/shadow store account information